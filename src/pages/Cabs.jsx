import {useSearchParams,Link} from 'react-router-dom'; import {Car,CheckCircle2} from 'lucide-react'; import Page from './PageTemplate'; import BookingForm from '../components/BookingForm'; import {images,services,whatsapp} from '../data/siteData';
export default function Cabs(){const[q]=useSearchParams();return <Page eyebrow="TIRUPATI CAB SERVICE" title="A cab for every sacred journey." text="Reliable local, outstation, airport and temple taxi service from Tirupati." image={images.taxi}><div className="content"><BookingForm/>{q.get('from')&&<p className="result">Your search: <b>{q.get('from')} to {q.get('to')}</b>. Select a service below or <a href={whatsapp}>message us on WhatsApp</a>.</p>}<section className="rental-banner"><div><p className="eyebrow">INSIDE TIRUPATI · RENTALS</p><h2>Need a car on rent?</h2><p>Compare available cars, travellers and buses with clear daily starting rates.</p></div><Link className="button" to="/cabs/rentals">View vehicle availability</Link></section><div className="card-grid">{services.map(([I,t,d])=><article key={t}><I/><h3>{t}</h3><p>{d}</p><Link to="/contact">Book now <CheckCircle2 size={15}/></Link></article>)}</div></div></Page>}
