import {Link} from 'react-router-dom'; import Page from './PageTemplate'; import {images,services} from '../data/siteData'; import {serviceLinks} from '../data/servicePages';
export default function Services(){return <Page eyebrow="TRAVEL SERVICES" title="Travel, your way." text="From a quick airport drop to a multi-day group tour, we keep travel uncomplicated." image={images.taxi}><div className="content"><div className="service-categories">{serviceLinks.map(x=><Link to={`/${x.slug}`} key={x.slug}>{x.title}</Link>)}</div><div className="card-grid">{services.map(([I,t,d])=><article key={t}><I/><h3>{t}</h3><p>{d}</p><Link to="/contact">Explore service</Link></article>)}</div></div></Page>}
